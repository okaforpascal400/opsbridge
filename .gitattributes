# The returns export is generated by the seed and committed, so its bytes must be
# identical on every platform. Without this, a checkout on Windows rewrites it to
# CRLF and the next seed run reports the whole file as modified.
data/returns.csv text eol=lf
